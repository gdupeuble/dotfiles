[user]
	email = gaetan.dupeuble@clearsy.com
	name = Gaetan Dupeuble
[core]
	autocrlf = false
	fileMode = false
	editor = /usr/bin/vim
[pull]
	ff = only
[alias]
	co = checkout
	ci = commit
	st = status
	br = branch
        dss = diff --shortstat
        rb = rebase
        fpt = fetch --prune --tags
        fe = fetch
        rh = reset --hard
        pf = push --force
	gbid = "if() { git grep -inP '(?<![A-Za-z0-9_])'\"$1\"'(?![A-Za-z0-9_])' -- ':!*.pmm'; }; f"
[push]
	default = simple
